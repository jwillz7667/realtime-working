# rename this to .env

TWILIO_ACCOUNT_SID="" # fill with AC... SID (remove quotes or place value inside)
TWILIO_AUTH_TOKEN="" # fill with auth token
# Optional: default caller ID for outgoing calls (E.164 format)
TWILIO_OUTBOUND_NUMBER=""
# Optional: explicit TwiML URL for realtime streaming; defaults to the websocket-server PUBLIC_URL
TWILIO_TWIML_URL=""
# Optional: override the realtime relay base URL (defaults to http://localhost:8081)
NEXT_PUBLIC_REALTIME_SERVER_URL=""
