{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npm run build --workspace=webapp",
  "devCommand": "npm run dev --workspace=webapp",
  "installCommand": "npm install --workspace=shared && npm install --workspace=webapp",
  "framework": "nextjs",
  "outputDirectory": "webapp/.next"
}
