[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = [
  "npm install --workspace=shared",
  "npm install --workspace=websocket-server"
]

[phases.build]
cmds = ["npm run build --workspace=websocket-server"]

[start]
cmd = "npm start --workspace=websocket-server"
